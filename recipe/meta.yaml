{% set version = "0.1.76" %}
{% set name = "jaxlib" %}

package:
  name: jaxlib
  version: {{ version }}

source:
  url: https://github.com/google/jax/archive/{{ name }}-v{{ version }}.tar.gz
  sha256: 6d5e185906dbc8f0f6589c5bc0569ae049bfe6346c271db73d5077f1fda41c7c
  patches:
    - 0001-Don-t-add-dashes-to-version.patch
    - 0001-Patch-pocketfft-for-C-17-osx-64.patch  # [osx and x86_64]

build:
  number: 0
  skip: true  # [win or py<37]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - scipy                                  # [build_platform != target_platform]
    - bazel 4.1.0      # [not (osx and arm64)]
    - bazel            # [osx and arm64]
    - bazel-toolchain
  host:
    - python
    - pip
    - numpy       # [py>=39]
    - numpy 1.19  # [py<39]
    - scipy
    - wheel
  run:
    - python
    - absl-py
    - {{ pin_compatible('numpy') }}
    - scipy
    - six
    - python-flatbuffers
  run_constrained:   # [osx]
    - __osx >={{ MACOSX_DEPLOYMENT_TARGET|default("10.9") }}  # [osx]

test:
  requires:
    - pip
  commands:
    - pip check
  imports:
    - jaxlib
    # Only this import actually triggers the load of XLA
    - jaxlib.xla_client

about:
  home: http://github.com/google/jax
  license: Apache-2.0
  license_file: LICENSE
  summary: 'Composable transformations of Python+NumPy programs: differentiate, vectorize, JIT to GPU/TPU, and more'
  dev_url: http://github.com/google/jax

extra:
  recipe-maintainers:
    - ericmjl
    - xhochy
